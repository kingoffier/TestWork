services:
  api:
    container_name: testwork-api
    build:
      context: ..
      dockerfile: TestWork.API/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Docker
      ConnectionStrings__DefaultConnection: "Server=db,1433;Database=TestWorkPriceTracker;User Id=sa;Password=${MSSQL_SA_PASSWORD:-TestWork_StrongPassword123!};TrustServerCertificate=True;Encrypt=False;MultipleActiveResultSets=True"
      Email__Enabled: "false"
    depends_on:
      db:
        condition: service_healthy

  db:
    container_name: testwork-db
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "1433:1433"
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: Developer
      MSSQL_SA_PASSWORD: "${MSSQL_SA_PASSWORD:-TestWork_StrongPassword123!}"
    volumes:
      - testwork_mssql_data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$${MSSQL_SA_PASSWORD}\" -C -Q \"SELECT 1\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

volumes:
  testwork_mssql_data:
